# Optional: override API URL. Leave commented for auto-detect (recommended on VPS).
# Auto uses https://your-domain.com/api — nginx/Apache must proxy /api → Node :5000
#
# Local dev only (if not using npm start proxy):
# REACT_APP_API_URL=http://localhost:5000/api
